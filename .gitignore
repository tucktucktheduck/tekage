node_modules/
# NOTE: tkg.html + library.html are the shareable built artifacts — tracked on
# purpose so the single playable file can be downloaded/played straight from
# GitHub. Rebuild them with `node scripts/build.mjs`.
# Aider scratch / history
.aider*
!.aider.conf.yml
!.aider.model.settings.yml
# Playwright
test-results/
playwright-report/
